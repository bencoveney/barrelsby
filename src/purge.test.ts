import {assert} from "chai";
import * as Mocha from "mocha";

import * as Purge from "./purge";

describe("purge module has a", () => {
    describe("purge function that", () => {
        xit("should do some logic", () => {
            Purge.purge(null, null);
        });
    });
});
