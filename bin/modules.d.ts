import { Logger } from "./options/logger";
import { Directory, Location } from "./utilities";
export declare function loadDirectoryModules(directory: Directory, logger: Logger, include: string[], exclude: string[], local: boolean): Location[];
